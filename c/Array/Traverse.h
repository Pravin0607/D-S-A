// time complexity of traversal of array is O(n).
void traverse(int arr[],int track)
{
    int i=0;
    printf("\ntraversing the array \nelements : ");
    for(i;i<track;i++)
    {
        printf("%d ",arr[i]);
    }
    printf("\n");
}